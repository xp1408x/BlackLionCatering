export interface EventItem {
  id: string;
  imageUrl: string;
  title: string;
  imageAlt: string;
  link: string; // Nueva propiedad link agregada
}

export interface GalleryImage {
  id: string;
  imageUrl: string;
  altText: string;
}
